// DEPRECATED:
// Character profiles are now managed dynamically via the UI and stored in localStorage.
// See `components/TranslationArea.tsx` for the new `NovelProject` structure.
export const FIXED_CHARACTER_PROFILE = "";