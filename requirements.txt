# Python dependencies for Chinese Translation Application
# All dependencies are from the Python standard library - no external packages needed
# This keeps the application lightweight and reduces dependency overhead

# For production use, consider adding:
# requests>=2.31.0  # For HTTP API calls
# redis>=5.0.0      # For distributed caching
# prometheus-client>=0.19.0  # For metrics collection
